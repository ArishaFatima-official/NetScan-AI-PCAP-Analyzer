# NetScan-AI-PCAP-Analyzer
Python-based network traffic analyzer that processes PCAP files, detects security threats, and creates a self-contained interactive security dashboard with visual analytics.
